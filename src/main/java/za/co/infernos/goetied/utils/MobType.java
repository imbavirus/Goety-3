package za.co.infernos.goetied.utils;

/**
 * Temporary mob-type compatibility enum used during 1.21 migration.
 * Vanilla MobType no longer exists in 1.21+, so Goetied logic maps to these buckets.
 */
public enum MobType {
    UNDEAD,
    ARTHROPOD,
    ILLAGER,
    WATER
}
