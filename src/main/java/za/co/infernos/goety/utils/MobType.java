package za.co.infernos.goety.utils;

/**
 * Temporary mob-type compatibility enum used during 1.21 migration.
 * Vanilla MobType no longer exists in 1.21+, so Goety logic maps to these buckets.
 */
public enum MobType {
    UNDEAD,
    ARTHROPOD,
    ILLAGER,
    WATER
}
